export default function Footer({ className }) {
  return (
    <footer
      className={`${className} mt-3 bg-white dark:bg-slate-800 h-16 px-3 lg:px-16 flex items-center justify-center border-t`}
    >
      Footer
    </footer>
  );
}
Footer.propTypes;
